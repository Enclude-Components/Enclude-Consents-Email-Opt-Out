minimum_cumulusci_version: '3.89.0'
project:
    name: Enclude-Consents-Email-Opt-Out
    package:
        name: Enclude-Consents-Email-Opt-Out
        api_version: '61.0'
    dependencies:
        - github: 'https://github.com/EncludeLtd/Consents'
    git:
        default_branch: 'main'
    source_format: sfdx

tasks:
    run_tests:
        options:
            required_org_code_coverage_percent: 75
